# Roles reference (moved)

The canonical role docs now live as individual pages under
[`docs/src/roles/`](src/roles/) and render in the mdbook (`just docs`).

This file used to duplicate that content and has drifted from reality several
times. It's now a redirect — start at
[`docs/src/roles/bitwarden.md`](src/roles/bitwarden.md) or use the sidebar in
the rendered book.
