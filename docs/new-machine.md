# Adding a new machine (moved)

The current procedure lives at
[`docs/src/onboarding.md`](src/onboarding.md) and uses `just onboard` /
`just add-machine` to handle bootstrap, inventory, and `host_vars` in one shot.

Render it via `just docs` and read it as part of the mdbook handbook.
