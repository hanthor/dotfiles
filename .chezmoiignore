# Ignore Zen Browser config on non-desktop machines
# Note: run_once_ scripts cannot be excluded here — guard added inside the script itself
{{- if not .isDesktop }}
.config/zen-browser
{{- end }}

# Fish files managed by fisher/fish itself (not chezmoi)
.config/fish/fish_variables
.config/fish/functions
.config/fish/completions
.config/fish/conf.d
