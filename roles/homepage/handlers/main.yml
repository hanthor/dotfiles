---
- name: Reload systemd daemon
  ansible.builtin.systemd:
    daemon_reload: true
    scope: system
  become: true

- name: Restart homepage
  ansible.builtin.systemd:
    name: homepage.service
    state: restarted
    scope: system
  become: true
