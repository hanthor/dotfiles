[Container]
ContainerName=appflowy-redis
Image=docker.io/library/redis:7-alpine
Exec=redis-server --requirepass {{ appflowy_redis_password }}
Network=appflowy

[Service]
Restart=always

[Install]
WantedBy=default.target
