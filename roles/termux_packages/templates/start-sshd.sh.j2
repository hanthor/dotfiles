#!/data/data/com.termux/files/usr/bin/sh
# Deployed by termux_packages role — runs on device boot via Termux:Boot.
# Requires the Termux:Boot and Termux:API companion apps to be installed
# separately (not available via pkg — install from F-Droid/GitHub releases).
termux-wake-lock
sshd
