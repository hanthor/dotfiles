# For agents

This repository IS the instruction set. Start at **[SKILL.md](SKILL.md)** and
follow its routing table — read only the one or two files your task needs.

Working ON this repo (not using it): keep progressive disclosure intact.
Skills stay self-contained and short, with YAML frontmatter (`name`,
`description` with trigger conditions). Teach in `skills/`, catalog in
`INDEX.md`, route in `SKILL.md` — never duplicate content across them. New
app audits follow `audits/TEMPLATE.md` and get integrated per its checklist.
