---
all:
  hosts:
    kerala:
      ansible_host: localhost
      ansible_connection: local
    vm:
      ansible_host: localhost
      ansible_connection: local
    karnataka:
      ansible_host: localhost
      ansible_connection: local
    bihar:
      ansible_host: localhost
      ansible_connection: local
    kanpur:
      ansible_host: localhost
      ansible_connection: local
    himachal:
      ansible_host: localhost
      ansible_connection: local
    dilli:
      ansible_host: localhost
      ansible_connection: local
    goa:
      ansible_host: localhost
      ansible_connection: local
    matrix:
      ansible_host: localhost
      ansible_connection: local
    lkofoss:
      ansible_host: localhost
      ansible_connection: local
  children:
    desktop:
      hosts:
        kerala:
        karnataka:
        kanpur:
        himachal:
        dilli:
        goa:
    llm:
      hosts:
        karnataka:
    server:
      hosts:
        vm:
        bihar:
    vps:
      hosts:
        matrix:
        lkofoss:
